# SQL legacy

Ces fichiers sont l'historique pré-CLI du schéma, appliqué à la main sur la base en mars 2026, avant la mise en place des migrations Supabase CLI.

Ils sont **dépréciés** et **consolidés** dans `supabase/migrations/20260307000000_baseline.sql`.

La source de vérité est désormais `supabase/migrations/` (voir `supabase/MIGRATIONS.md`).

Ne jamais rejouer ces fichiers.
